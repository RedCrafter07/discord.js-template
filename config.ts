export const TOKEN = 'TOKEN'; //replace with your token
export const GLOBAL = false; //if true, commands will be registered to all guilds.
export const MAIN_GUILD = 'GUILD_ID'; //only required if GLOBAL is false.
